extends layout

block content

  h1 Biberon ID #{biberon._id}
  script.
    var validation= function(){
      var quantite = document.getElementById("inputQuantite").value;
      var dob = document.getElementById("inputDob").value;
      if (quantite === ""){
        alert("Merci de remplir la quantite");
        return false;
        }
      else if (quantite <0 || quantite > 500){
        alert("Quantité incorrecte");
        return false;
        }
        else
          return true;
      };

  form(action='/biberons/#{biberon._id}/edit',method='post',name='updatebiberon',enctype='application/x-www-form-urlencoded')
    p Quantite:
      input#inputQuantite(type='number', value='#{biberon.quantite}', name='quantite')
    p DOB:
      input#inputDob(type='date', value='#{biberondob}', name='dob')
    p
      input(type='hidden',value='PUT',name='_method')
    p
      button#btnSubmit(type='submit', onclick="return validation()").
                Modifier